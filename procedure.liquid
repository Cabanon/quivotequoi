---
pagination:
    data: procedures
    size: 1
    alias: procedure
permalink: "procedure/{{ procedure.reference }}"
---
<!DOCTYPE html>
<meta charset="utf8" />
<html>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jsdiff/5.2.0/diff.min.js"></script>
<link
  rel="stylesheet"
  href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.classless.min.css"
/>
<body>
    <header><h1>QuiVoteQuoi?</h1></header>
    <main>
        <section>
            <a href="/">⟵ Retourner à la recherche</a>
            <h2>{{ procedure.title }}</h2>
            {% assign procedure_votes = votes | where: 'procedure_ref', procedure.reference %}
            <ul>
                {% for vote in procedure_votes %}
                <li><a href="/vote/{{ vote.id }}">{{ vote.title }}</a></li>
                {% endfor %}
            </ul>
        </section>
    </main>
</body>
</html>